function a(param1, param2){
    console.log("The sum of numbers are", param1+param2);
}

a(2,3);

// Parameters : The placeholder assigned while declaring a function is known as parameters. They defines how many values are required while
// calling the function and they act like a local variable(inside the functions local scope) for that function 

// Arguments : The values passed to the function while calling them are known as arguments, these values takes the place of parameters and
// initialize them with the passed value.